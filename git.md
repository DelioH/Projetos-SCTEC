# Ve se o s dados estão ok
git pull
# adicionar algo
git add .
# para saber o que não foi atualizado. escrevr uma linha
git status
git commit -m "teste"

# para enviar
git push

# para enviar
######

#Iniciar a pasta para receber o git
git init

# para clonar
git clone https://github.com/RGBRUNINI/filmes_imdb.git

# desfazer o comando
# pegar o odigo do ommig 
git revert <codigo do commit>
git push
git add .
git commit -m "reversao"

# atualizar de acordo com as coisas novas d apasta online
git pull origin main
